from dlpm_forecast_loop import run_DLPM_loop

# run_DLPM_loop(run_name='DLPM initial run')
run_DLPM_loop(run_name='DLPM initial run', hierarchy_lvl='category')
run_DLPM_loop(run_name='DLPM initial run', hierarchy_lvl='subcategory')
